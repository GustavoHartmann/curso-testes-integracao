describe("test calculator functions", () => {
  it("test sum", () => {
    expect(1 + 2).toBe(3);
  });

  it("test sub", () => {
    expect(1 - 2).toBe(-1);
  });

  it("test mul", () => {
    expect(1 * 2).toBe(2);
  });

  it("test div", () => {
    expect(1 / 2).toBe(0.5);
  });
});
